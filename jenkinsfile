pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Sadashiva555/Shiva.git'
            }
        }
        
       stage('Build') {
            steps {
                
                   sh 'mvn clean compile'
                
           }
        }
        
   }
}